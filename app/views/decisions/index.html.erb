<%= render "layouts/nav" %>

<% if notice!=nil %>
<div class="column is-12 ">
    <div class="notification is-warning">
      <button class="delete"></button>
      <%= notice %>
    </div>
</div>
<% end %>

<div class="column is-12 ">
  <div class="box cta">
    <div class="columns is-mobile is-centered">
        <div class="field is-grouped is-grouped-multiline">
          <div class="control">
            <%= link_to new_decision_path ,class: "button is-link " do%>
              <span class="icon"><i class="fa fa-plus" aria-hidden="true"></i></span><span></span>Tambah Kesimpulan</a>
            <% end %>
          </div>
        </div>
    </div>
  </div>
</div>

<div class="column is-12 ">
  <div class="box cta">
    <div class="column is-12">
    <div class="table__wrapper" >
      <table class="table is-narrow is-hoverable is-fullwidth">
        <thead>
          <tr>
            <th>Judul Kesimpulan</th>
            <th class="has-text-centered">Aksi</th>
          </tr>
        </thead>
        <tbody>
          <% @decisions.each do |decision| %>
          
          <tr>
            <td data-link="<%= decision_path(decision)%>"><%= decision.title %></td>
            <td class="has-text-centered">
              <%= link_to decision, method: :delete, data: { confirm: 'Anda yakin?' } ,class: "icon has-text-danger" do%>
                <i class="fa fa-trash" aria-hidden="true"></i>
              <% end %>
            </td>
          </tr>
          <% end %>
        </tbody>
      <table>
      </div>
    </div>
  </div>
</div>

<script>
$("td[data-link]").click(function() {
  window.location = $(this).data("link")
})
</script>

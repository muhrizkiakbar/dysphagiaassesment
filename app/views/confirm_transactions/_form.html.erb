<%= form_with(model: confirm_transaction, local: true) do |form| %>
  <% if confirm_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(confirm_transaction.errors.count, "error") %> prohibited this confirm_transaction from being saved:</h2>

      <ul>
      <% confirm_transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :transaction_id %>
    <%= form.text_field :transaction_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
